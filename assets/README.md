# Assets

Keep editable source assets here. Generated binaries belong in `build/`.

Planned subdirectories: `tiles/`, `sprites/`, `portraits/`, `maps/`, `palettes/`, `music/`, `sfx/`, and `dialogue/`.

