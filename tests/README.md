# Tests

The first automated addition should be a host-native build of `protocol_dice.c` that verifies die bounds, blank faces, deterministic seeds, and statistical uniformity within a documented tolerance. Emulator smoke tests should record emulator/ROM versions and fixed-seed input scripts.

