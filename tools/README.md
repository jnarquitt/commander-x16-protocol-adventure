# Host Tools

This directory will contain deterministic converters and validators for PNG tiles/sprites, Tiled maps, dialogue tokens, encounter tables, palettes, audio, compression, stable IDs, bank packing, and the final memory report.

